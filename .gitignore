# Folder
Plans/

# .NET build output
samples/**/bin/
samples/**/obj/

# SQLite databases generated by samples
samples/**/*.db
samples/**/*.db-shm
samples/**/*.db-wal

# Build-fail demo: the .cs version is for testing only — keep .cs.broken under VCS
samples/**/BuildFailDemo/*.cs

# Generated debug reports
debug-report-*.html

# testing-explorer generated artifacts
# (generated report archives live under .report/ — do NOT add a broad
#  test-report-*.html rule, it would ignore the skill's own
#  assets/test-report-template.html)
.claude/skills/testing-explorer/.report/
skills/testing-explorer/.report/
**/TestResults/
**/coverage.cobertura.xml
**/playwright-report/
**/test-results/
samples/playwright-sample/node_modules/
pw-results.json
# Sample fixtures ship WITHOUT coverage tooling so the skill exercises auto-provision
samples/**/dotnet-tools.json
samples/**/.config/dotnet-tools.json

# agent-debate-team: runtime session state + generated reports
.agent-team/
agent-debate-report-*.html

# Temporary debug logs from api-flow-debugger
server-debug*.log
server-debug.clean.log
build-output.log

# Claude local settings (personal, not shared)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
